<div class="acc-additionals">
<?php
    foreach ($this->container as $pages) {
        // loop next level
        foreach ($pages as $uid => $page) { 
           if ($page->getLabel() !== null && $page->isVisible(true)) {?>
           <div class="acc-nav">
            <?php echo $this->menu()->htmlify($page);?>
            </div>
        <?php } }
    }
?>
</div>


